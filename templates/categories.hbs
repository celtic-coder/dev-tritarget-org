{{> header}}

<div class="page-header">
  <h1>Posts about {{ tag }}</h1>
</div>

<div class="panel panel-default">
  <div class="list-group">
  {{#each posts}}
    {{> blog_list_item}}
  {{/each}}
  </div>
</div>

{{> footer}}
